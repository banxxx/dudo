import 'package:flutter/widgets.dart';

class ReaderSelectionLayer extends StatelessWidget {
  const ReaderSelectionLayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
