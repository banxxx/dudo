import 'package:flutter/widgets.dart';

class ReaderHighlightLayer extends StatelessWidget {
  const ReaderHighlightLayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
