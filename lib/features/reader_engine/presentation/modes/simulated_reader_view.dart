import 'package:flutter/widgets.dart';

class SimulatedReaderView extends StatelessWidget {
  const SimulatedReaderView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
